<Window x:Class="FastShot.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FastShot — Settings"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">

  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="#CDD6F4"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize"   Value="13"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background"  Value="#313244"/>
      <Setter Property="Foreground"  Value="#CDD6F4"/>
      <Setter Property="BorderBrush" Value="#45475A"/>
      <Setter Property="Padding"     Value="6,4"/>
      <Setter Property="FontSize"    Value="13"/>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#CDD6F4"/>
      <Setter Property="FontSize"   Value="13"/>
    </Style>
    <Style TargetType="Button">
      <Setter Property="Background"  Value="#89B4FA"/>
      <Setter Property="Foreground"  Value="#1E1E2E"/>
      <Setter Property="FontWeight"  Value="SemiBold"/>
      <Setter Property="FontSize"    Value="13"/>
      <Setter Property="Padding"     Value="14,6"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor"      Value="Hand"/>
    </Style>
  </Window.Resources>

  <StackPanel Margin="20">
    <TextBlock Text="⚙️ FastShot Settings" FontSize="17" FontWeight="Bold" Margin="0,0,0,18"/>
    <TextBlock Text="Global Hotkey" Margin="0,0,0,4"/>
    <TextBox x:Name="HotkeyBox" Margin="0,0,0,4"/>
    <TextBlock Text="e.g.  Ctrl+Shift+S   or   Ctrl+Alt+P" FontSize="11" Foreground="#6C7086" Margin="0,0,0,14"/>
    <TextBlock Text="Save Folder" Margin="0,0,0,4"/>
    <Grid Margin="0,0,0,14">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBox x:Name="FolderBox"/>
      <Button Grid.Column="1" Content="Browse" Margin="6,0,0,0"
              Background="#585B70" Foreground="#CDD6F4"
              Click="Browse_Click"/>
    </Grid>
    <CheckBox x:Name="SaveToDiskChk" Content="Save screenshot to disk" Margin="0,0,0,8"/>
    <CheckBox x:Name="ShowToastChk"  Content="Show notification after capture" Margin="0,0,0,20"/>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Margin="0,0,10,0"
              Background="#585B70" Foreground="#CDD6F4"
              Click="Cancel_Click"/>
      <Button Content="Save Changes" Click="Save_Click"/>
    </StackPanel>
  </StackPanel>
</Window>
